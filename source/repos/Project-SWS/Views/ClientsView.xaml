<UserControl x:Class="Project_SWS.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Imię:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewClientFirstName}" Width="100" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

            <TextBlock Text="Nazwisko:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewClientLastName}" Width="100" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

            <TextBlock Text="Telefon:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewClientPhone}" Width="100" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

            <TextBlock Text="Email:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewClientEmail}" Width="120" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

            <Button Content="Dodaj" Command="{Binding AddClientCommand}" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Usuń wybrane" Command="{Binding DeleteClientCommand}" Padding="10,5" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}" AutoGenerateColumns="True" IsReadOnly="True" />
    </Grid>
</UserControl>