<UserControl x:Class="Project_SWS.Views.CarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_SWS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Marka:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewCarBrand}" Width="100" Margin="0,0,15,0" VerticalAlignment="Center"/>

            <TextBlock Text="Model:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewCarModel}" Width="100" Margin="0,0,15,0" VerticalAlignment="Center"/>

            <TextBlock Text="Rocznik:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewCarYear}" Width="60" Margin="0,0,15,0" VerticalAlignment="Center"/>

            <TextBlock Text="Cena/Dzień:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding NewCarPrice}" Width="60" Margin="0,0,15,0" VerticalAlignment="Center"/>

            <Button Content="Dodaj" Command="{Binding AddCarCommand}" Height="35" Margin="0,0,10,0"/>
            <Button Content="Usuń wybrane" Command="{Binding DeleteCarCommand}" Height="35" Style="{StaticResource MaterialDesignOutlinedButton}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Cars}" SelectedItem="{Binding SelectedCar}" AutoGenerateColumns="True" IsReadOnly="True" />
    </Grid>
</UserControl>