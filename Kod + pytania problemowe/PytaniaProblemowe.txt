1. Premia dla pracowników za obsłużone wysyłki
Opis problemu:
Firma chce przyznać premię pracownikom w zależności od liczby wysyłek obsłużonych w danym miesiącu.

Kroki:
1.Wyszukać pracowników, którzy realizowali wysyłki w danym miesiącu.
2.Policzyć liczbę wysyłek dla każdego pracownika.
3.Na podstawie liczby wysyłek wyznaczyć procent premii.

===============================FUNKCJA===============================

CREATE OR REPLACE FUNCTION public.calculate_employee_bonus(
    p_month INT,
    p_year INT
)
RETURNS TABLE (
    employee_id BIGINT,
    shipments_count INT,
    bonus_percent INT
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        s.employee_id,
        COUNT(*)::INT AS shipments_count,
        CASE
            WHEN COUNT(*) > 100 THEN 30
            WHEN COUNT(*) BETWEEN 50 AND 100 THEN 15
            ELSE 0
        END AS bonus_percent
    FROM public.shipments s
    WHERE EXTRACT(MONTH FROM s.shipment_date) = p_month
      AND EXTRACT(YEAR  FROM s.shipment_date) = p_year
      AND s.employee_id IS NOT NULL
    GROUP BY s.employee_id;
END;
$$;

===============================Wywolanie:===============================

SELECT *
FROM public.calculate_employee_bonus(10, 2024);

====================================================================================================

2. Automatyczne oznaczanie przeterminowanych wysyłek
Opis problemu:
Wysyłki starsze niż 7 dni i niezrealizowane powinny zostać automatycznie oznaczone jako przeterminowane.

Kroki:
1.Wyszukać wysyłki starsze niż 7 dni.
2.Sprawdzić ich aktualny status.
3.Zaktualizować status na OVERDUE.

===============================PROCEDURA===============================

CREATE OR REPLACE PROCEDURE public.mark_overdue_shipments()
LANGUAGE plpgsql
AS $$
BEGIN
    UPDATE public.shipments
    SET status = 'OVERDUE'
    WHERE shipment_date < CURRENT_DATE - INTERVAL '7 days'
      AND status <> 'DELIVERED';
END;
$$;

===============================Wywolanie===============================

CALL public.mark_overdue_shipments();

===============================Wynik===============================
SELECT shipment_id, shipment_date, status
FROM public.shipments
ORDER BY shipment_id;

====================================================================================================

3. Wykrywanie braków magazynowych
Opis problemu:
System powinien wskazywać produkty, których ilość w magazynie spadła poniżej ustalonego minimum.

Kroki:
1.Pobranie aktualnych stanów magazynowych.
2.Porównanie ilości z progiem minimalnym.
3.Zwrócenie produktów wymagających uzupełnienia.

===============================FUNCKJA===============================

CREATE OR REPLACE FUNCTION public.find_low_stock_products(
    p_min_quantity NUMERIC
)
RETURNS TABLE (
    product_id BIGINT,
    location_id BIGINT,
    quantity NUMERIC
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        i.product_id,
        i.location_id,
        i.quantity
    FROM public.inventory i
    WHERE i.quantity < p_min_quantity;
END;
$$;

===============================Wywolanie:===============================

SELECT *
FROM public.find_low_stock_products(10::NUMERIC);

====================================================================================================

4. Obliczenie stopnia realizacji dostawy
Opis problemu:
Chcemy określić procent realizacji danej dostawy (receipt).

Kroki:
1.Pobranie expected_quantity i received_quantity.
2.Zsumowanie ilości dla całej dostawy.
3.Obliczenie procentu realizacji.

===============================FUNCKJA===============================

CREATE OR REPLACE FUNCTION public.receipt_completion_percentage(
    p_receipt_id BIGINT
)
RETURNS NUMERIC
LANGUAGE plpgsql
AS $$
DECLARE
    expected_sum NUMERIC := 0;
    received_sum NUMERIC := 0;
BEGIN
    SELECT
        COALESCE(SUM(expected_quantity), 0),
        COALESCE(SUM(received_quantity), 0)
    INTO expected_sum, received_sum
    FROM public.receipt_details
    WHERE receipt_id = p_receipt_id;

    IF expected_sum = 0 THEN
        RETURN 0;
    END IF;

    RETURN ROUND((received_sum / expected_sum) * 100, 2);
END;
$$;

===============================Wywolanie:===============================

SELECT public.receipt_completion_percentage(1);

====================================================================================================

5. Najaktywniejsi klienci w danym okresie
Opis problemu:
Firma chce zidentyfikować klientów generujących największą liczbę wysyłek.

Kroki:
1.Pobranie wysyłek z danego okresu.
2.Zliczenie wysyłek dla klientów.
3.Wybranie klientów przekraczających ustalony próg.

===============================FUNCKJA===============================

CREATE OR REPLACE FUNCTION public.get_top_clients(
    p_from DATE,
    p_to DATE,
    p_min_shipments INT
)
RETURNS TABLE (
    client_id BIGINT,
    shipments_count INT
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        s.client_id,
        COUNT(*)::INT AS shipments_count
    FROM public.shipments s
    WHERE s.shipment_date BETWEEN p_from AND p_to
    GROUP BY s.client_id
    HAVING COUNT(*) >= p_min_shipments;
END;
$$;

===============================Wywolanie:===============================

SELECT *
FROM public.get_top_clients('2024-01-01', '2024-12-31', 1);

====================================================================================================

6. Kontrola pojemności lokalizacji magazynowej
Opis problemu:
System powinien sprawdzić, czy dana lokalizacja magazynowa nie została przepełniona.

Kroki:
1.Zsumowanie ilości produktów w lokalizacji.
2.Pobranie maksymalnej pojemności lokalizacji.
3.Porównanie wartości i zwrócenie wyniku.

===============================FUNKCJA===============================

CREATE OR REPLACE FUNCTION public.check_location_capacity(
    p_location_id BIGINT
)
RETURNS BOOLEAN
LANGUAGE plpgsql
AS $$
DECLARE
    total_quantity NUMERIC := 0;
    v_max_capacity NUMERIC;
BEGIN
    SELECT COALESCE(SUM(i.quantity), 0)
    INTO total_quantity
    FROM public.inventory i
    WHERE i.location_id = p_location_id;

    SELECT l.max_capacity
    INTO v_max_capacity
    FROM public.locations l
    WHERE l.location_id = p_location_id;

    -- jeśli brak limitu, uznajemy że OK
    IF v_max_capacity IS NULL THEN
        RETURN TRUE;
    END IF;

    RETURN total_quantity <= v_max_capacity;
END;
$$;

===============================Wywolanie:===============================

SELECT public.check_location_capacity(1);

====================================================================================================

7. Aktualizacja stanu magazynu po przyjęciu dostawy
Opis problemu:
Po przyjęciu dostawy system musi zaktualizować stany magazynowe.

Kroki:
1.Pobranie danych z receipt_details.
2.Dopasowanie produktów do inventory.
3.Aktualizacja ilości i daty modyfikacji.

===============================PROCEDURA===============================

CREATE OR REPLACE PROCEDURE public.update_inventory_after_receipt(
    p_receipt_id BIGINT
)
LANGUAGE plpgsql
AS $$
BEGIN
    UPDATE public.inventory i
    SET quantity = i.quantity + rd.received_quantity,
        last_updated = NOW()
    FROM public.receipt_details rd
    WHERE rd.receipt_id = p_receipt_id
      AND rd.product_id = i.product_id;
END;
$$;

===============================Wywolanie===============================

CALL public.update_inventory_after_receipt(1);

===============================Wynik===============================

SELECT product_id, quantity, last_updated
FROM public.inventory
ORDER BY product_id;

====================================================================================================

8. Ocena produktywności pracowników
Opis problemu:
Chcemy określić produktywność pracowników na podstawie liczby obsłużonych klientów.

Kroki:
1.Pobranie wysyłek pracowników.
2.Zliczenie unikalnych klientów.
3.Określenie poziomu produktywności.

===============================FUNKCJA===============================

CREATE OR REPLACE FUNCTION public.employee_productivity()
RETURNS TABLE (
    employee_id BIGINT,
    clients_count INT,
    productivity_level TEXT
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        s.employee_id,
        COUNT(DISTINCT s.client_id)::INT AS clients_count,
        CASE
            WHEN COUNT(DISTINCT s.client_id) > 20 THEN 'HIGH'
            WHEN COUNT(DISTINCT s.client_id) BETWEEN 10 AND 20 THEN 'MEDIUM'
            ELSE 'LOW'
        END AS productivity_level
    FROM public.shipments s
    WHERE s.employee_id IS NOT NULL
    GROUP BY s.employee_id;
END;
$$;

===============================Wywolanie===============================

SELECT *
FROM public.employee_productivity()
ORDER BY employee_id;

====================================================================================================

9. Najczęściej wysyłane produkty
Opis problemu:
Firma chce zidentyfikować produkty kluczowe pod względem wysyłek.

Kroki:
1.Pobranie danych z shipment_details.
2.Zsumowanie wysyłanych ilości.
3.Wybranie produktów przekraczających próg.

===============================FUNKCJA===============================

CREATE OR REPLACE FUNCTION public.most_shipped_products(
    p_min_quantity NUMERIC
)
RETURNS TABLE (
    product_id BIGINT,
    total_quantity NUMERIC
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        sd.product_id,
        SUM(sd.quantity_to_ship) AS total_quantity
    FROM public.shipment_details sd
    GROUP BY sd.product_id
    HAVING SUM(sd.quantity_to_ship) >= p_min_quantity;
END;
$$;

===============================Wywolanie===============================

SELECT *
FROM public.most_shipped_products(1000::NUMERIC);

====================================================================================================

10. Walidacja poprawności dostawy
Opis problemu:
System powinien sprawdzić poprawność danych dostawy przed jej zatwierdzeniem.

Kroki:
1.Pobranie danych o ilościach oczekiwanych i odebranych.
2.Sprawdzenie poprawności danych.
3.Zgłoszenie błędu lub zatwierdzenie dostawy.

===============================FUNCKJA===============================
CREATE OR REPLACE FUNCTION public.validate_receipt(
    p_receipt_id BIGINT
)
RETURNS BOOLEAN
LANGUAGE plpgsql
AS $$
BEGIN
    IF EXISTS (
        SELECT 1
        FROM public.receipt_details
        WHERE receipt_id = p_receipt_id
          AND received_quantity > expected_quantity
    ) THEN
        RETURN FALSE;
    END IF;

    RETURN TRUE;
END;
$$;

===============================Wywolanie:===============================

SELECT public.validate_receipt(12);


